<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive data from cloud backup -->
        <exclude domain="sharedpref" path="password_prefs.xml" />
        <exclude domain="database" path="password_database" />
    </cloud-backup>
    <device-transfer>
        <!-- Allow device transfer but exclude sensitive data -->
        <exclude domain="sharedpref" path="password_prefs.xml" />
        <exclude domain="database" path="password_database" />
    </device-transfer>
</data-extraction-rules>